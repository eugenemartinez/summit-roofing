{
  "name": "wordpress-theme",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite build --watch\" \"wp-scripts start --output-path=src/theme/blocks --source-path=src/blocks\"",
    "build": "vite build && wp-scripts build --source-path=src/blocks --output-path=src/theme/blocks",
    "package": "npm run build && node scripts/package-theme.js",
    "package:zip": "npm run build && node scripts/package-theme.js --zip",
    "link": "node scripts/symlink.js",
    "unlink": "node scripts/symlink.js --remove",
    "clean": "rm -rf node_modules release; rm -rf node_modules release && echo 'cleaned'"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "@wordpress/scripts": "^31.6.0",
    "concurrently": "^9.2.1",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "motion": "^12.38.0",
    "tailwindcss": "^4.2.1"
  }
}
